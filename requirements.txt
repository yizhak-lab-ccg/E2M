joblib>=1.3
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
requests>=2.31
scipy>=1.11
scikit-learn>=1.3
torch>=2.1
xgboost>=1.7

# pyproject.toml is authoritative; this file mirrors the core runtime dependencies.
# XGBoost is a core dependency, so `e2m tmb` and XGBoost SHAP work after a plain install.
# For the SHAP library (used by lightgbm/random_forest/gradient_boosting/neural SHAP),
# install the optional extra:  pip install -e ".[interpretation]"
